# Mirrors the [project.dependencies] list in pyproject.toml — kept as a
# separate file because most PaaS Python buildpacks (Render included) look
# for requirements.txt by default. If you add/change a dependency, update
# both files together.
#
# Local dev still uses `pip install -e .[dev]` against pyproject.toml; this
# file is for production deploys where only the base app needs to run.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
websockets>=13
numpy>=1.26,<2.1
scipy>=1.13
networkx>=3.3
matplotlib>=3.9
bloqade-analog>=0.16
qutip>=5.0
python-dotenv>=1.0
