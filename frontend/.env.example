# Copy to .env.production (or .env) and fill in for a split deployment
# (e.g. Vercel frontend + Render backend). Leave unset for local dev —
# vite.config.ts's dev-server proxy handles /api and /ws against
# localhost:8000 automatically.

VITE_API_URL=https://your-backend.onrender.com
